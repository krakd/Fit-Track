name: Server CI/CD

on:
  push:
    branches:
      - main
      - dev
    paths:
      - 'server/**'
  pull_request:
    branches:
      - main
      - dev
    paths:
      - 'server/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        working-directory: ./server
        run: npm ci
      
      - name: Set up Docker Buildx
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/setup-buildx-action@v2
      
      - name: Login to Docker Hub
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and push Docker image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        working-directory: ./server
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/fittrack-server:latest .
          docker build -t ${{ secrets.DOCKER_USERNAME }}/fittrack-server:${{ github.sha }} .
          docker push ${{ secrets.DOCKER_USERNAME }}/fittrack-server:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/fittrack-server:${{ github.sha }}
      
      - name: Build summary
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: echo "✅ Server Docker image pushed successfully!"